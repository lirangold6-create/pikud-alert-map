# Dependencies
node_modules/

# Logs
*.log
collector.log

# Data files (generated at runtime)
collected-alerts.json
collected-tzevaadom.json
collected-waves.json
training-data.json
training-output.txt

# Model files (INCLUDED in live-hosting branch for deployment)
# Trained locally and committed to avoid training on free tier
# model/ - commented out for live-hosting branch

# Package lock (optional - uncomment if you want to track it)
# package-lock.json
